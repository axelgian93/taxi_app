// Shared app configuration constants

// Poll interval (seconds) for rider to fetch driver location
const int kDriverPollSec = 5;

// Interval (seconds) for driver to send location updates when sharing
const int kDriverShareSec = 15;

